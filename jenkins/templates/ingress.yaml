# {{- if .Values.ingress.enabled }}
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: jenkins-ingress
#   annotations:
#     {{- toYaml .Values.ingress.annotations | nindent 4 }}
# spec:
#   rules:
#   - host: {{ .Values.ingress.host }}
#     http:
#       paths:
#       - path: {{ .Values.ingress.path }}
#         pathType: Prefix
#         backend:
#           service:
#             name: jenkins
#             port:
#               number: {{ .Values.service.port }}
# {{- end }}
{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jenkins-ingress
  annotations:
    {{- toYaml .Values.ingress.annotations | nindent 4 }}
spec:
  ingressClassName: nginx
  rules:
  - host: {{ .Values.ingress.hostName }}
    http:
      paths:
      - path: {{ index .Values.ingress.paths 0 "path" }}  # FIXED
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.service.name | default "jenkins" }}
            port:
              number: {{ .Values.service.port }}
{{- end }}
